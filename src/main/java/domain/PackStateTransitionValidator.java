package domain;

import entities.pack.Pack;
import entities.pack.PackState;

public class PackStateTransitionValidator {
    public boolean isTransitionAllowed(Pack pack, PackState desiredState) {
        throw new UnsupportedOperationException("implement me!");
    }
}
